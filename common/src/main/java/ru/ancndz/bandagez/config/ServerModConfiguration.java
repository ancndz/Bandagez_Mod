package ru.ancndz.bandagez.config;

public abstract non-sealed class ServerModConfiguration<T> extends ModConfiguration<T> {

    private static final class Options {
    }

    protected ServerModConfiguration() {
        // Server configuration can be initialized here if needed
    }
}